import {
  makeStyles,
  mergeClasses,
  shorthands,
  makeStaticStyles,
  makeResetStyles,
  createDOMRenderer,
} from "@fluentui/react-components";
export {
  makeStyles,
  mergeClasses,
  shorthands,
  makeStaticStyles,
  makeResetStyles,
  createDOMRenderer,
};
